#include <iostream>

using namespace std;

int main(int argc, char** argv) {
  // Input - the Input Layer
  Neuron *n = new Neuron(0.9);
  Neuron *n = new Neuron(0.5);
  Neuron *n = new Neuron(0.1);

  // Output
  Neuron *outputNeuron = new Neuron(0.0);

  // e.g. 90% confidence that the given input is a Dog

  return 0;
}